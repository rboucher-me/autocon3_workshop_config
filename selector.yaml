ping_scan_selector:
  selector:
    ping_discovery: true
  policies:
    default:
      path: ping_scan.yaml

port_scan_selector:
  selector:
    port_discovery_type: standard
  policies:
    default:
      path: port_scan.yaml
      
port_scan_fast_selector:
  selector:
    port_discovery_type: fast
  policies:
    default:
      path: port_scan_fast.yaml
